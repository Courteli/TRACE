# v9 纠错验证版：未来代码位置

状态：预留实现位置。目前此目录仅有这份说明，尚无模型实现、可运行配置、训练脚本或新测试。不要把根目录原 v9 的启动命令当作本版入口。

在后续明确实施时，本目录计划放置独立的 `run.py`、`src/`、`scripts/` 和 `tests/`。目录结构需与实际实现保持一致；现在未创建这些空模块，也未复制原 v9 冒充改进版。

基线位置为仓库根目录 `main/native_v9/`，数据/环境共享仓库根目录资源；新模型代码和配置只在本版本内演进。未来运行输出放入 `runs/v9_correctness_validation/<new_run_id>/`，不放进本目录或原正式 run。

实施要求见 [设计说明](../DESIGN.md)，验收见 [ACCEPTANCE.md](../ACCEPTANCE.md)。添加真实实现后必须更新 [STATUS.json](../STATUS.json) 和本说明，记录代码提交与验证证据。
