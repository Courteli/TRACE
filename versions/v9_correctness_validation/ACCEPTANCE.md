# 纠错验证版验收清单

状态：以下项目全部待实施/待执行。方框未勾选就是未完成，原 v9 的 15 项测试通过不代表本版已通过。

## 纯修复回归测试

- [ ] F1：固定动作、噪声和执行模式，省略 view 与显式零号 view 的输入、latent、答案 log-prob 一致。
- [ ] F1：rollout、replay、SFT replay、生成和严格验证都使用同一默认条件。
- [ ] F2：关闭 replay/其他辅助梯度源，非零答案优势能使 COMMIT 专属均值头获得有限非零梯度。
- [ ] F2：前七个 forced actions 数值不变；COMMIT 不参与随机动作 loss，其动作由当前网络重算。
- [ ] F3：零噪声不改变输出或额外推进 RNG；非零噪声作用于约定位置，固定 seed 可重现。
- [ ] F3：评测/受控 RL 重放不带未记录噪声；恢复状态包含后续计算所需 RNG。
- [ ] F4：小词表下的采样器分布与 old/current log-prob 对应；生成前缀与打分前缀一致。
- [ ] F4：包括 generation_config 继承项在内，所有概率处理均显式审计。
- [ ] F4：PAD、EOS、左填充、提前停止和长度截断掩码正确；追加 PAD 不改变损失。
- [ ] 更新前同一条件 old/current 概率在容差内一致，更新后按预期变化，old 概率保持固定。

## 单独的信用分配测试

- [ ] D1：只有末评分非零，紧前随机角色收到 gamma 倍评分；更前位置有正确折扣。
- [ ] D1：gamma=1、八个评分全为 1 时，首随机角色回报为 8；末角色策略 mask 仍为 False。
- [ ] D1：答案奖励没有重复计数；不同问题的 group 不相互泄漏。
- [ ] 明确纯修复和加终端过程信用两种配置的身份；不把 D1 结果仅归因于 bug 修复。

## 集成、谱系与结果门槛

- [ ] 单卡和 DDP 的有效样本/token reduction 与所写公式一致。
- [ ] Stage1 参考仅在正确加载后初始化；恢复时不被当前策略意外覆盖，参数保持冻结。
- [ ] 保存/恢复包含优化器、调度器、参考模型、RNG 和必要 loop 状态；受控下一步与不中断计算一致。
- [ ] 区分 Stage2 兼容诊断与重新训练 Stage1 的完整纠正版，记录所有派生权重来源。
- [ ] 四卡训练期 padding 与最终 747 唯一验证题协议分开审计，不改正式数据字节。
- [ ] 独立小规模 GPU 验收使用单独授权的资源和输出目录，不干扰原正式运行。
- [ ] 若实际训练，保存独立提交、配置、预算、seed、结果和失败记录，不转用旧版本成绩。

每项通过后应补充实现 commit、测试命令、执行日期与结果位置，再更新 STATUS.json；现在不提供未经运行的通过率。
