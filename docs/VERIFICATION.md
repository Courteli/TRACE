# 整理验证记录（2026-09-07）

本记录针对上传的独立源码与数据副本，不是模型效果报告。

| 检查 | 结果 |
| --- | --- |
| CPU unittest | 15 项通过；未加载完整基础权重、未启动 GPU 训练 |
| 正式数据 | 15 个 JSON/JSONL 文件均可解析，大小及 SHA-256 与原文件相同 |
| 主版本源码 | 85 个文件记录逐文件哈希；模型、模块及 run.py 核心代码零修改 |
| 配置可移植性 | 22 个 YAML 在环境变量展开后仅数据/模型路径变化，其他配置语义相同 |
| 历史源码 | 11 个目录、1241 个文件的复制前后树哈希相同 |
| 基础模型参考文件 | 9 个配置/tokenizer/许可/索引文件通过大小和 SHA-256 校验；权重不在仓库中 |
| 语法 | 主版本与打包工具 45 个 Python 文件、8 个 shell 文件通过检查 |
| 上传安全扫描 | 未检出常见访问令牌或私钥模式，未包含受排除的权重/checkpoint/日志类型，无超过 50 MiB 的文件 |
| 便携入口 | `scripts/run_native_v9.sh --dry-run` 通过；只打印命令，没有分配 GPU 或运行训练 |

复查命令：

```bash
python tools/audit_repository.py
bash scripts/test_cpu.sh
```

CPU 测试使用原有 ROT 环境；未验证新建环境的全量安装和三阶段 GPU 训练。全环境已知的可选视频依赖缺项、基础权重来源 revision 尚未确认、v9 专门消融队列尚不完整等边界，见环境、消融与复现说明。模式扫描不是完整的安全证明，私密仓库也不应保存密钥。
